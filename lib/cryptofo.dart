import 'package:flutter/material.dart';

class Cryptofo extends StatelessWidget {
  const Cryptofo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Cryptofo')),
    );
  }
}
